# njsQ - NodeJS Quick

Import njsQ by doing the following:

install the package:
  ```js
  npm install github:Newfies/njsq
  ```
  
define the package:
  ```js
  const njsQ = require('NodeJSQuick');
  ```
